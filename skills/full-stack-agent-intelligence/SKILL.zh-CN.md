---
name: full-stack-agent-intelligence
description: "用于整体优化长期运行 agent：信息、调度、loop、输出流、记忆、认知、进化、审计和信任架构。"
version: 1.0.0
---

# 全面智能架构

当用户要求“全面提升智能”“认知架构总整理”“GA/Hermes 是否满足要求”“继续优化 agent 灵性”时，使用这个 skill。

## 总图

```text
信息 -> 调度 -> loop -> 输出流

记忆架构 -> 认知架构 -> 进化架构 -> 审计架构 -> 信任架构

记忆 -> 技能 -> 方法论 -> 印象
```

## 核心原则

主链路要干净：

```text
用户输入 -> 场景/目的 -> 相关上下文 -> agent loop -> 工具/行动 -> 输出
```

认知层要像直觉一样影响注意力、联想和行动，不要变成 pending、准入、强确认、固定元命令或审批门。

## 判断顺序

1. 先看信息是否完整：消息、文件、工具、cron、报告、运行事实是否有来源、时间和场景。
2. 再看调度是否自然：短事短答，大事开工，学习材料直接学习，dream 旁路复盘。
3. 再看 loop 是否闭环：计划、执行、验证、汇报、沉淀。
4. 再看输出是否像人：闲聊不用卡片，长任务有任务、动作、Outputs、结论。
5. 再看记忆是否分层：L0-L5 是否继承关键证据。
6. 再看进化是否有验证：学到的东西是否变成 memory/skill/method/persona。
7. 再看审计是否减噪：发现退化，而不是增加约束。
8. 最后看信任是否足够：agent 是否被允许为了用户长期变好主动行动。

## 落地规则

- 信息层只做证据底座，不全量塞 prompt。
- 调度层只路由注意力，不当权限门卫。
- loop 层必须做完事，不能只有工具调用。
- 输出层隐藏 raw trace，展示用户需要的动作、结果和结论。
- 记忆层按 L0-L5 升层，不能摘要到失真。
- 认知层维护 Purpose、Attention、Association、Action、Feedback、Dream。
- 进化层要有来源、验证、资产落点和汇报。
- 审计层检查退化、串场、遗忘和新摩擦。
- 信任层默认给 agent 观察权、联想权、补位权、提醒权、推进权、滋养权和自我维护权。

## 资产去向

| 内容 | 去向 |
|---|---|
| 原始事实和事件 | memory / cognitive events / wiki |
| 外部未验证模式 | agent-systems-patterns |
| 实战验证方法 | how-to-agent |
| 可执行工作流 | skills |
| 用户长期偏好 | USER / PERSONA / active memory |
| 柔性关系理解 | impressions |
| 自身源码修复 | agent repo + tests + git commit |

## 完成标准

用户说“继续”“下一步”“学一下”“你看着办”时，agent 能接住上下文、选对输出形式、主动推进、验证结果、记住自己做过什么，并把经验变成下次更好的行为。
