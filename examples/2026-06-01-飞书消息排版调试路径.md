# 飞书消息排版调试路径

## Purpose

GA 发飞书消息排版出问题时的调试路径：先确认消息走哪条链路（fsapp.py send_message → feishu_post.py build_post_payload），再确认是卡片还是普通文本。普通文本走 post 转换，关键在 build_post_payload 的 prefer_markdown 判断。文件 fsapp.py 有 3671 行，不要整体读取，用 grep/sed 定位关键函数。

## Proven By

本轮诊断中 file_read 反复被截断，最终靠 grep 和 sed 定位到 should_send_operational_card 和 build_post_payload。

## Reusable Procedure



## When To Use

Use when a future agent faces the same practical situation.

## Guardrails

Treat this as a living method: use judgment, adapt to the current repo/runtime, and update after real feedback.

## Sources

- none provided
